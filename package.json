{
  "name": "ai-workflow-routines",
  "version": "1.0.0",
  "description": "Workflow routines for GitHub issues",
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test dist/bin/*.test.js",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "minimist": "^1.2.8"
  },
  "devDependencies": {
    "@types/minimist": "^1.2.5",
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2"
  }
}
