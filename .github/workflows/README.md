# send-metrics.yml

Auto-sends close phase data to Honeycomb after PR merge.

**Setup**: Add `HONEYCOMB_API_KEY` secret in repo Settings → Secrets and variables → Actions.

That's it—runs automatically on every PR merge.
